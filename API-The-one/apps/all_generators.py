

def graphic_generator_dict(data_dict: dict):

    for i in data_dict:
        yield i


def statistic_generator_dict(data_dict: dict):

    for i in data_dict:
        yield i


def others_generator_dict(data_dict: dict):

    for i in data_dict:
        yield i
